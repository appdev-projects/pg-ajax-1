var added_comment = $("<%= escape_javascript(render('comments/comment', comment: @comment)) %>");
var form_id = "#<%= dom_id(@comment.photo) %>_new_comment_form"

$(form_id).before(added_comment);

added_comment.hide();
added_comment.slideDown();

$(form_id+" #comment_body").val("");
